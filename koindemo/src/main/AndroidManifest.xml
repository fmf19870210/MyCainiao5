<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cniao5.koindemo">

    <application
        android:name=".KoinDemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyCainiao5">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                  <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".factory.FactoryActivity" />
        <activity android:name=".single.SingleActivity" />
        <activity android:name=".viewmodel.ViewModelActivity" />
        <activity android:name=".normal.NormalActivity" />
        <activity android:name=".normal.NormalTwoActivity" />
        <activity android:name=".libmodule.LibModuleActivity" />
        <activity android:name=".fragment.FragActivity"/>
        <activity android:name=".other.OtherActivity"/>
    </application>

</manifest>